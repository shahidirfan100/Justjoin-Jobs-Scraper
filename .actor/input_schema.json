{
  "title": "JustJoin Jobs Scraper",
  "description": "Extract job listings from JustJoin.it - Poland's leading tech job board. Filter by keyword, location, and workplace preference.",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "startUrls": {
      "title": "🔗 Start URLs",
      "type": "array",
      "description": "Optionally provide specific JustJoin.it search URLs to scrape.",
      "editor": "requestListSources",
      "prefill": [],
      "default": []
    },
    "keywords": {
      "title": "🔍 Search Keywords",
      "type": "string",
      "description": "Search term for job titles or skills (e.g., 'React', 'Python', 'DevOps').",
      "editor": "textfield",
      "prefill": "",
      "default": ""
    },
    "city": {
      "title": "📍 City",
      "type": "string",
      "description": "Filter by city name (e.g., 'Warszawa', 'Kraków', 'Wrocław').",
      "editor": "textfield",
      "prefill": "",
      "default": ""
    },
    "workplaceTypes": {
      "title": "🏢 Workplace Type",
      "type": "array",
      "description": "Filter by work location preference.",
      "editor": "select",
      "items": {
        "type": "string",
        "enum": [
          "remote",
          "hybrid",
          "office"
        ]
      },
      "prefill": [],
      "default": []
    },
    "collectDetails": {
      "title": "📝 Collect Full Details",
      "type": "boolean",
      "description": "Fetch complete job descriptions and application links. Disable for faster listing-only scrapes.",
      "editor": "checkbox",
      "prefill": true,
      "default": true
    },
    "maxItems": {
      "title": "🔢 Maximum Jobs",
      "type": "integer",
      "description": "Maximum number of job listings to extract.",
      "minimum": 1,
      "maximum": 10000,
      "prefill": 50,
      "default": 50,
      "editor": "number"
    },
    "maxPages": {
      "title": "📄 Maximum Pages",
      "type": "integer",
      "description": "Maximum number of result pages to process.",
      "minimum": 1,
      "maximum": 100,
      "editor": "number",
      "prefill": 10,
      "default": 10
    },
    "proxyConfiguration": {
      "title": "🌐 Proxy Configuration",
      "type": "object",
      "description": "Enable Apify Proxy for better reliability and to avoid blocking.",
      "editor": "proxy",
      "prefill": {
        "useApifyProxy": true
      },
      "default": {
        "useApifyProxy": true
      }
    }
  },
  "required": [],
  "additionalProperties": false
}